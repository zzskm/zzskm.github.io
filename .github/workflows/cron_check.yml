name: cron-check
on:
  schedule:
    - cron: "*/5 * * * *"   # 5분마다 (UTC)
  workflow_dispatch: {}

jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "event: $GITHUB_EVENT_NAME"
          date -u
          echo "repo: $GITHUB_REPOSITORY, sha: $GITHUB_SHA"
